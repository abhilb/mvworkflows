name: "test"
description: |
    Task <b>test</b> is for testing.
    It has the following the types of parameters.
    <ul>
    <li>Boolean Parameter - That takes either True or False</li>
    <li>String Paraeter - That takes a string as input</li>
    <li>Double Parameter - That takes a floating pointe value.</li>
    </ul>
parameters:
    - parameter: bool_param
      value: True
      type: BOOL_PARAM
      default: False
    - parameter: str_param
      value: "dummy"
      type: STR_PARAM
      default: "default"
    - parameter: double_param
      value: 2.001
      type: DOUBLE_PARAM
      default: 3.0
properties:
    - number_provider: True
    - number_list_provider: False
    - feature_provider: False
    - string_provider: True
    - image_provider: False
