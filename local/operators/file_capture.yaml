name: "file_capture"

description: |
    Reads images from the give folder.

parameters:
- parameter: file_path
  value: "d:\\images\\"
  type: FILE_PATH_PARAM
  default: "d:\\"

outputs:
    - name: image
      type: IMAGE
